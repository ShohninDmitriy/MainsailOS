[ -n "$FLUIDD_DEPS" ] || FLUIDD_DEPS="nginx"
[ -n "$FLUIDD_URL" ] || FLUIDD_URL=https://github.com/fluidd-core/fluidd/releases/latest/download/fluidd.zip
# mainsail.cfg repo
[[ -n "$FLUIDD_CONFIG_REPO_SHIP" ]] || FLUIDD_CONFIG_REPO_SHIP="https://github.com/fluidd-core/fluidd-config.git"
[[ -n "$FLUIDD_CONFIG_REPO_BRANCH" ]] || FLUIDD_CONFIG_REPO_BRANCH="master"
# mainsail.cfg setup
[[ -n "$FLUIDD_CONFIG_SRC_PATH" ]] || FLUIDD_CONFIG_SRC_PATH="/home/${BASE_USER}/fluidd-config/fluidd.cfg"
[[ -n "$FLUIDD_CONFIG_DEST_PATH" ]] || FLUIDD_CONFIG_DEST_PATH="/home/${BASE_USER}/printer_data/config/fluidd.cfg"
